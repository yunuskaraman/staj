import { LibreofficeRunnerService } from "./services/libreoffice-runner.service";
export declare class DocxToPdfConverter {
    private readonly libreOfficeRunner;
    constructor(libreOfficeRunner: LibreofficeRunnerService);
    convert(sourceFilePath: string, destinationFilePath: string): Promise<boolean>;
}
