/// <reference types="node" />
import { Writable } from 'stream';
import { ExtractOptions, Extract } from 'tar-fs';
export declare class TarDecompressor {
    private readonly decompress;
    constructor(decompress: (cwd: string, opts?: ExtractOptions) => Extract);
    getConverter(destination: string): Writable;
}
