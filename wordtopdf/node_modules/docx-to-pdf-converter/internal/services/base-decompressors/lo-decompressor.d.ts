import { FileSystemService } from "../file-system.service";
import { TarDecompressor } from "./tar.decompressor";
import { BrotliDecompressor } from "./brotli.decompressor";
export declare class LODecompressor {
    private readonly fileSystemService;
    private readonly brotliConverter;
    private readonly tarDecompressorConverter;
    constructor(fileSystemService: FileSystemService, brotliConverter: BrotliDecompressor, tarDecompressorConverter: TarDecompressor);
    decompress(compressedFilePath: string, destination: string): Promise<string>;
}
