/// <reference types="node" />
import { Duplex } from 'stream';
export declare class BrotliDecompressor {
    private readonly decompressMethod;
    constructor(decompressMethod: () => Duplex);
    getConverter(): Duplex;
}
