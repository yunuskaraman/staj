import { LODecompressor } from "./base-decompressors/lo-decompressor";
import { FileSystemService } from "./file-system.service";
export declare class LoExecutableGetter {
    private readonly loDecompressor;
    private readonly fileSystemService;
    private readonly currentDir;
    private readonly loExecutablePath;
    private readonly loCompressedPath;
    constructor(loDecompressor: LODecompressor, fileSystemService: FileSystemService, currentDir: string, loExecutablePath: string, loCompressedPath: string);
    getExecutablePath(): Promise<string>;
    private prepareBinary;
}
