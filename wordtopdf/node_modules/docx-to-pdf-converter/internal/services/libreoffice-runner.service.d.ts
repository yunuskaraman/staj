import { CommandExecuterService } from "./command-executer.service";
import { LoExecutableGetter } from "./lo-executable-getter";
export declare class LibreofficeRunnerService {
    private readonly loExecutableGetter;
    private readonly commandExecuter;
    private readonly libreOfficeArgs;
    constructor(loExecutableGetter: LoExecutableGetter, commandExecuter: CommandExecuterService, libreOfficeArgs: string[]);
    convertToPdf(docxFilePath: string, pdfDestinationPath: string): Promise<void>;
    private runConvertingProcess;
}
